# Magnify — session log

Append-only, newest first. Every working session adds one entry at the TOP: date, what changed, any infra facts touched (database, domain, auth, secrets). Infra changes also go into `CLAUDE.md` immediately, not just here.

## 2026-08-02 — v2.38.1: web/PWA safe-area regression — viewport-fit=cover (postbuild)

- Scott reported (with screenshots) the Gathered bar + content sliding under the iPhone status bar on the installed app. Root cause on web: Expo's web template emits a viewport meta WITHOUT `viewport-fit=cover`, so `env(safe-area-inset-top)` is 0 — and react-native-safe-area-context's web provider measures exactly that CSS value, so `insets.top` was 0 and the v2.31.1 `paddingTop: insets.top + 6` fix collapsed on web. Native was unaffected.
- Fix: `scripts/postbuild.js` now appends `viewport-fit=cover` to the generated viewport meta (guarded, idempotent). Verified locally by running postbuild against dist.
- Same session fixed the sibling bug in Conduct (v0.10.1) and Liken (v0.9.1): their switcher bars were missing the `env(safe-area-inset-top)` padding Steward added in `5db0290`. Glean/Knit/Tidings already had it.
- Pushed to main (`dad0145b`); Vercel rebuilds the web export.

- Web-only fix: on viewports ≥1024px the app rendered full-bleed edge-to-edge. Added step 5 to `scripts/postbuild.js` — injects a `<style id="desktop-layout">` block into the exported `dist/index.html` (`#root` max-width 900px + `margin: 0 auto`, body background `#f7f8fb`, all inside `@media (min-width: 1024px)`). Below 1024px and on native nothing changes (postbuild only touches the web export).
- Verified locally: `npm run build:web` succeeds, CSS present in dist, screenshots at 1280px (centered column) and 1000px (unchanged full-bleed).
- No infra touched. State: v2.37.1 pushed to main → Vercel deploy. Pre-existing uncommitted `app.json` versionCode bump left untouched.

## 2026-07-15 — Doc system initialized (history reconstructed from git)

- 2026-03-28: initial scaffold on the shared Supabase project — schema deliberately preserved Sparkle Pro's existing tables; Magnify's own tables are unprefixed.
- 2026-03-29 → 04-18: rapid v1.x→v2.x buildout — full EN/ES i18n, sustaining/release script generator, Slack webhooks per board, permissions overhaul, deep links.
- 2026-04-29 → 05-25: stake-suite design system adoption, new brand marks, Gathered 5-app switcher, Web Push home-screen badges (v2.16), web/desktop parity (Phase 6, v2.24–2.25).
- 2026-05-20: VAPID push secrets recovered after a cross-app overwrite in the shared secret namespace (#19) — origin of the "grep before `supabase secrets set`" rule.
- 2026-06-08: migrated to magnify.gatheredin.app; fixed Magnify↔Sparkle `profiles` leak by scoping the shared table with an `app` column; EAS build/submit pipeline set up (ASC 6778263386).
- 2026-06-09 → 06-15: expo-updates OTA added, Church disclaimer on sign-in, security hardening (v2.33), High Council roster management (v2.34–2.35), "Try the demo" one-tap Demo Mode (v2.37.0), Gradle 9 Android build patch.
- 2026-07-05 → 07-06: Gathered super-admin RLS for HC roster; multi-tenant Stage 1 tenant foundation (additive); /install.html PWA install page.
- 2026-07-08: demo account locked down from confidential church data (RLS); multi-tenant Stage 2 (`stake_id` + RLS re-key) committed but marked NOT YET APPLIED to the database.
- State at initialization: v2.37.0, 155 commits, live at magnify.gatheredin.app, working tree has one uncommitted `app.json` change (left untouched).
- 2026-08-08: v2.39.0 — standalone Release workflow (presidency-only entry type; SP-approval queue → SP-president-only advance → straight to HC Sustain, badged RELEASE; `is_release` column + presidency-only insert RLS applied to prod) + one-tap Slack approval-reminder buttons on both boards (HC → hc_stage_change channel, SP → sp_stage_change). Note: multi-tenant Stage 2 (marked "not yet applied" on 07-08) WAS applied 2026-07-09 along with invites/delegated approval (v2.38.0). Also diagnosed missing native push: the App Store build has no push at all — pipeline is Web Push only (`lib/push.ts`, no expo-notifications); native APNs support is an open work item.
- 2026-08-08 (later): v2.40.0 — native push for store builds (expo-notifications + Expo Push API; tokens in `magnify_native_push_tokens`; edge fn v33 sends badge-always + banner-on-increase, language-aware; ALL expo-notifications imports lazy so OTA bundles don't crash pre-push binaries). Android-from-website = PWA: sw.js now shows a real notification when the Badging API is missing (Android Chrome) instead of doing nothing. Desktop web returned to FULL WIDTH (900px column from v2.37.1 removed at the source, scripts/postbuild.js — documented in CLAUDE.md; do not reintroduce). BLOCKED on Scott: `eas login` needed to (1) `eas update` the OTA (native apps are stale — missing Release feature + safe-area fix) and (2) build/submit the new push-capable iOS binary.
- 2026-08-08 (cont.): v2.41.0 — release scope is now an explicit Ward/Stake choice (was inferred from the ward picker, which sent a high-councilor release to one ward instead of all). Adam Hunter's release record corrected to stake_calling. EAS wired for fully non-interactive operation via EXPO_TOKEN + ASC API key + APPLE_TEAM_ID (see CLAUDE.md); published two production OTA updates (v2.40.1, v2.41.0) to runtime 1.0.0. iOS: enabled PUSH_NOTIFICATIONS on the App ID via ASC API, deleted the invalidated provisioning profile, EAS minted a push-capable one, build #11 succeeded and was submitted to App Store Connect. REMAINING: no APNs push key in the Expo account — must be created in the Apple portal (no API exists) and uploaded before iOS push delivers.
- 2026-08-08 (end): APNs push key created in the Apple portal (browser-driven): "Left Field Apps Push", Key ID P4F5BY7BW2, Sandbox & Production, Team Scoped — reusable across every Left Field app. Backed up to ~/.appstoreconnect/private_keys/ (Apple keeps no copy). Registered with EAS and assigned to Magnify. iOS push chain is now COMPLETE: App ID capability + push-capable provisioning profile (YD86Z4C77N) + build #11 in App Store Connect + APNs key. Remaining before users get notifications: Apple's processing, then submit build 11 for review and release.
- 2026-08-08 (final): v2.42.0 — stake-calling sustaining now runs PARALLEL to the stage instead of gating it. The HC Sustain column and each ward's script keep showing a stake calling until that ward has sustained it, even after the card advances to set_apart/record (`isSustainingOnly` in HCKanbanScreen); cards show "Sustained in X of Y wards" + ALREADY SET APART; detail page gained a logged "mark remaining wards sustained" override. One record, one audit trail — deliberately NOT duplicate cards. Published as OTA to production.
- 2026-08-08 (root cause): the "fleet stuck on 2.29.0" mystery SOLVED by building a simulator replica of store build 7 (same commit/channel) and reading expo-v11.db + unified logs: every OTA ever published exported with EMPTY EXPO_PUBLIC_* env (eas update --environment production reads EAS's server env store, which was empty — not .env) → "supabaseUrl is required" fatal at JS startup → expo-updates ErrorRecovery silently rolled back and blacklisted each update. Fixed: env vars stored in EAS production environment; v2.42.1 republished with env verified INSIDE the bundle (strings|grep); replica then applied it successfully (ok=1/fail=0, "Try the demo" visible on 2.29-era binary). Build 13 (iOS 1.1.0, runtime pinned 1.0.0, native push) submitted to ASC. QC rule extended: an OTA counts as delivered only when a device APPLIES it — serving is not delivery.
- 2026-08-08 (crash + true fix): the 2.42.x "crashes immediately after opening" was native push. On the old build-7 binary (no expo-notifications native module, no aps entitlement) merely REQUIRING expo-notifications wires up a native emitter at import that HARD-CRASHES on a real device — invisible in the simulator, which is why it shipped. Fix (lib/nativePush.ts): gate on expo-modules-core requireOptionalNativeModule('ExpoPushTokenManager') BEFORE importing expo-notifications; absent → fully inert. VERIFIED by injecting the demo Supabase session (applereview@…) into a sim replica's AsyncStorage (RCTAsyncLocalStorage_V1/manifest.json) + magnify.demoMode=on and booting straight to signed-in — app initialized clean, no exception, ExpoPushTokenManager registered & gated. ARCHITECTURE fix: emergency rollback-to-embedded keeps the build-7 fleet (runtime 1.0.0) safely on 2.29.0; we no longer OTA new native code onto it. Build 15 (version 1.1.0, RUNTIME BUMPED to 1.1.0 so future OTAs never reach build 7) carries everything + working push; auto-submitted to TestFlight for real-device verification by Scott before App Store release. Build 13 (pre-fix) superseded — do not release.
- 2026-08-08 (deploy): App Store v1.1.0 SUBMITTED (build 16 = HEAD w/ native-push fix, runtime 1.1.0), phased release armed, WAITING_FOR_REVIEW. Real-device verification finally achieved via AD-HOC internal distribution after TestFlight refused to serve the build (Apple backend; agreements/compliance/SDK all ruled out) and EAS non-interactive ad-hoc credential creation failed: created a distribution cert + ad-hoc provisioning profile via ASC API + openssl, fed to EAS via credentials.json (credentialsSource=local, `.adhoc-*` + credentials.json gitignored), built internal profile → install link → Scott enabled iOS Developer Mode, installed build 17, tested, approved. Fleet still safe on 2.29.0 (rollback) until 1.1.0 approves. NOTE: 1.1.0 runtime is 1.1.0 (isolated from the build-7 fleet's 1.0.0 OTA lane on purpose).
- 2026-08-11: v2.43.0 — (1) calling_notes append-only comment thread (any approved same-stake user incl. high councilors can add; delete only own; RLS + demo_block) on the detail screen beside the admin-only Notes field. (2) Smarter SP action badges in BOTH ActionCountsContext + edge fn v34 (kept in sync): Ideas badge only the Stake President and never his own; For-Approval badges each counselor until they approve and badges the president only after BOTH counselors approve; clerk/exec-sec dropped from for_approval. Verified against live data before deploy (president for_approval 13→6, ideas 0). Also diagnosed Miguel's "Adam Frost repeat" = two legitimate callings (June Asst Ward Clerk complete + Aug Ward Clerk at set_apart), not a bug. Shipped: web (Vercel, live-verified) + OTA to runtime 1.1.0 (env-inlining verified in the exported bundle — supabase URL present). Build-7 fleet (runtime 1.0.0) unaffected, still on the 2.29.0 rollback.
- 2026-08-16: v2.43.1 — keyboard no longer covers the focused field on the web/PWA build (Scott reported it on New Entry → Notes, with a screenshot). Root cause: react-native-web does NOT implement the keyboard APIs the native side uses — its `Keyboard` module is a stub whose `addListener` never fires and whose `isVisible()` always returns false, and `KeyboardAvoidingView` renders a plain `View` with an empty `onKeyboardChange`. So on the PWA *nothing* avoided the keyboard, including the four auth screens that look protected because they wrap themselves in a KeyboardAvoidingView. Fix: `lib/useKeyboardInset.ts` derives the covered height from the VisualViewport API (layout viewport minus `visualViewport.height + offsetTop`; ignores <80px so URL-bar chrome isn't mistaken for a keyboard, caps at 70%), plus `useScrollFocusedFieldIntoView` which scrolls the nearest scrollable ancestor by an exact delta — `scrollIntoView({block:'center'})` was tried first and is WRONG here because it centres within the container, whose centre is roughly where the keyboard starts (measured: field landed 19px under the keyboard line). New `components/ui/KeyboardAwareScrollView.tsx` wraps ScrollView, appends a spacer of exactly the covered height (a trailing spacer, NOT `paddingBottom` on contentContainerStyle — the longhand would override the `padding` shorthand these screens use and silently drop their side/top padding), and sets `automaticallyAdjustKeyboardInsets` on iOS only. Native is deliberately untouched: iOS handles it via that prop, Android via adjustResize, so the hook returns 0 there and nothing double-counts. Applied to NewCalling, CallingDetail (+ decline/edit bottom sheets, lifted by `paddingBottom: inset`), HighCouncil, SlackSettings, SuggestionFAB, and all five auth screens. Verified in headless Chromium against the real exported bundle with a faked visualViewport: focused field moved from y=360 (keyboard top y=250 — covered) to y=234, spacer 250px on open / 0px on close. No infra touched.
- 2026-08-17: v2.44.0 — approval reminders got their own Slack destination. Before: the one-tap "Send reminder" buttons on the HC and SP boards posted to `hc_stage_change` / `sp_stage_change`, i.e. the same callings channels that carry the whole stage-change feed, so reminders landed in the noisiest channel. Now `lib/slack.ts` has a `webhooksFor(eventTypes)` helper that resolves a PRIORITY list (first event type with an active webhook wins, not a union) and the reminders resolve `hc_reminder` → `hc_stage_change` and `sp_reminder` → `sp_stage_change`. Two new rows in Settings → Slack Notifications (now 7 slots, driven by a `SLACK_EVENTS` array instead of an inline literal + a hardcoded "of 5"), each with a hint explaining the fallback. No schema change needed: `slack_settings.event_type` is free text with no CHECK and no unique constraint (verified against the live DB, which holds 6 rows: hc_stage_change, sp_stage_change, rejection, user_access, user_access_request, user_access_approved). Until Scott pastes the two council-channel webhook URLs in Settings, reminders keep going exactly where they do today. No infra touched.
- 2026-08-17 (cont.): verified the webhook→channel mapping directly in the Slack workspace rather than trusting the row names, because Scott expected the app to already have direct-channel webhooks. Result: `sp_stage_change` → #stakepresidencycallings, `hc_stage_change` → #highcouncilcallings, `rejection` → **#stake-presidency** (the presidency's own channel — the "Magnify: Rejected" posts land there, so a direct-channel webhook DID already exist). #high-council has no Magnify bot posts at all in its history; the only Magnify traffic there is Scott hand-typing "please take a look at the Magnify board" (2026-08-08), i.e. exactly the message this feature automates. Acted on that: inserted an `sp_reminder` row on the live DB (project isogetmvnpimcmouakeg) copying the rejection row's webhook_url + stake_id via INSERT…SELECT (guarded by NOT EXISTS, `channel_name='stake-presidency'`, secret never surfaced), so SP reminders hit #stake-presidency the moment v2.44.0 deploys. `hc_reminder` is deliberately left empty — a Slack Incoming Webhook for #high-council has to be created in the Slack app config (no API for it here), and until then HC reminders keep falling back to #highcouncilcallings. Also corrected the misleading "Posts to SP channel only" comment in `lib/slack.ts`.
- 2026-08-18: v2.44.1 — the iPhone app froze when editing a calling. Scott hit it trying to switch a for_approval card from ward calling to stake calling: the Calling dropdown never opened and the sheet stopped responding. Root cause is NOT the calling list — it is that "Edit Calling Details" opened the ward and calling pickers as **`Modal`s nested inside the already-presented edit `Modal`**, which iOS cannot do (a UIViewController cannot present over one that is already presented), so the picker never appeared and touches were swallowed. Confirmed with Scott that BOTH dropdowns froze, which rules the calling list out. Could not reproduce on web at all — magnify.gatheredin.app and the Supabase host are both blocked by this session's egress policy (403 on CONNECT), so instead the real bundle was built (`npm run build:web`), served from localhost, and driven in Chromium with every Supabase call mocked via Playwright `route()` plus a seeded `sb-<ref>-auth-token` and `magnify.demoMode=on` — booting straight into `/calling/demo-call-014`, the fixture card that mirrors Scott's (ward_calling, for_approval, a calling_name not in CALLING_GROUPS so it opens as custom "Other"). That harness passed on desktop and iPhone-emulated viewports, slow and fast (taps inside the 250ms modal slide animation), with and without the keyboard focused first, and with a focus-event counter to rule out an RNW focus-trap loop — i.e. web was never broken, matching the iOS-only report. Fix: the edit sheet is now ONE Modal with three panes (`editPane: 'form' | 'ward' | 'calling'`); the pickers render in place with a "‹ Cancel" back link, `dismissEditLayer()` makes back/scrim tap return to the form before closing, and edits survive picking because the Modal never unmounts. Verified in the harness: 15/16 assertions pass (the one "failure" is a bad assertion — the New Calling screen mounted below in the navigator also contains the string "Member Name"). Swept every other screen for the same trap: no other nested Modal exists in the app. NOTE: this is an iPhone-app bug, so it only reaches users via `eas update` (JS-only, runtime 1.1.0) — no EAS token in this container, so Scott must publish.
- 2026-08-18 (cont.): v2.45.0 — the sustaining script now groups like callings. Scott was reading the Westchester 1st script for the next day and found several people called to the same position scattered through the list. Cause: `generateScript` in HCKanbanScreen mapped one line per card (`{member} as {calling}`) over `callings`, which the board fetches `order('created_at', ascending: false)` — newest card first, so people called to the same position appear wherever they happened to be entered. New `groupByCalling()` collapses entries sharing a calling into one ("A, B, and C as <calling>"), sorts groups by calling name and names within a group, and is applied to BOTH the sustainings motion and the releases motion (releases group by the calling being vacated). Deliberate call: the calling title is NEVER pluralized — they are free-text titles in two languages (real examples in the data include "Stake YSA Committee", "First Counselor in Stake Primary" and a hand-typed "1st counselor eq"), and a guessed plural gets read aloud over the pulpit. Verified by transpiling the two shipping functions straight out of the .tsx with the TS compiler and running them over the live Westchester 1st sustain-stage set: 8 individual entries collapse to 5, the three YSA committee members become one line, the two seminary instructors another, "Stake YSA Committee Co-Chair" correctly stays separate (different title), and every person still appears exactly once. Also drove the real exported bundle in Chromium (demo mode, Supabase mocked) through HC Board → Sustaining Script → ward picker to confirm the modal still renders with no page errors. Note for future work: the ward/stake mix matters — those YSA rows are a mix of `ward_calling` and `stake_calling`, and grouping is by calling name across both, which is what makes the one-line reading work.
- 2026-08-22: PR #21 merged to main as a merge commit (`2ded4b7`), deliberately NOT squashed so v2.44.0 / v2.44.1 / v2.45.0 stay independently revertable — they were three unrelated changes sharing one branch. Vercel rebuilds web from main. STILL OUTSTANDING for a human: (1) `eas update --environment production` to get the v2.44.1 iPhone edit-sheet fix onto the App Store build (JS-only, runtime 1.1.0 — no EAS token in the session container); (2) create a Slack Incoming Webhook for **#high-council** and paste it into Settings → Slack Notifications → "HC Approval Reminders", the only piece of the reminder routing that could not be finished from here. Also, one live data correction at Scott's request: a Westchester 1st release card in the sustain stage carried a hand-typed `release_current_calling` of "1st counselor eq", which the sustaining script reads aloud verbatim; corrected to "First Counselor in Elders Quorum Presidency" to match the card's own calling_name (single-row UPDATE guarded on the old value). Worth noting for the future: `release_current_calling` is free text with no picker behind it, so it will keep collecting variants like this — a picker there is the real fix.
- 2026-08-22 (cont.): v2.46.0 — MP ordinations now enter at the Stake Presidency approval queue instead of being created directly into HC Approval. Two places had to change, not one: (1) `NewCallingScreen` set `stage = type === 'mp_ordination' ? 'hc_approval' : 'ideas'` on insert — now MP and releases both enter at `for_approval`; (2) `CallingDetailScreen`'s edit-save re-aligned the stage when a card's type was changed TO mp_ordination, force-jumping anything in ideas/for_approval/stake_approved to `hc_approval` — that re-alignment is now gone (a converted card stays put) and only the issue_calling/ordained → sustain hop remains, since Extend Calling is the one stage MP still skips. Nothing else needed touching: the SP board query, `ActionCountsContext`, and edge fn `magnify-send-action-pushes` are all stage-based and type-agnostic, so MP cards badge the presidency and post to the SP Slack channel automatically — no edge function redeploy. `getNextStage`/`getPrevStage` already chained for_approval → stake_approved → hc_approval → (MP) sustain, so the pipeline needed no change. Copy updated in EN + ES: `new.submitToSPApproval` (new), `log.mpCreated`, `help.mpOrdinationBody`. Also fixed the post-submit confirmation, which sent everyone to the HC board even though nothing this screen creates lands there any more — now routes to the SP board, falling back to HC for high councilors, who have no SP tab (`showPresidencyBoard = isPresidency || isClerk`). Verified by driving the real exported bundle with demo mode OFF and Supabase mocked, capturing the actual POST to /rest/v1/callings: `stage: "for_approval"`, type and ordination_type intact, no page errors. DELIBERATELY NOT MIGRATED: 2 MP cards currently sitting in hc_approval and 2 in set_apart were left alone — they are mid-pipeline and moving them backwards would surprise the councils. If Scott wants those 2 hc_approval cards pulled back for presidency approval, that is a one-line UPDATE.
- 2026-08-23: v2.47.0 — callings can no longer leave the presidency queue unassigned. Scott's report: cards were being advanced past For Approval with empty task slots and then stalling downstream with nothing indicating who owed the next step. `handleAdvance` in CallingDetailScreen now blocks the `for_approval` transition when an assignable task slot is empty and shows a dialog naming exactly which ones. Two design decisions worth keeping: (1) the slot definition was hoisted out of `TaskAssignmentsSection` into module-level `taskFieldsFor()` / `unassignedTaskFields()`, so the assignments UI and the gate cannot drift — previously the MP "no extend_by" rule lived only in the section's render filter; (2) **`record_by` is deliberately NOT gated.** It is `locked` — display-only, auto-derived from whoever holds the stake clerk role, with no way to set it from that screen — so blocking on it would be a dead end with no button to press; a missing clerk is a roster problem fixed in Settings. Scott listed "record" in the request, so this is a deliberate deviation, flagged to him. The gate runs BEFORE the `isDemo` short-circuit so training behaves identically. That exposed a second problem: the demo seeded `spMembers`/`hcMembers` as empty arrays, so the assignee picker had nobody in it and a demo (and the App Review account, which is demo-locked) would have dead-ended permanently at For Approval — the demo branch of `fetchData` now seeds three fictional SP members and three fictional high councilors. Never seed real members there. Verified in the real exported bundle driven headlessly: advance blocked with the three expected slots listed and Record absent, card stayed in For Approval, then after assigning all three it advanced to Stake Approved with no second dialog and no page errors (9/9 assertions).
